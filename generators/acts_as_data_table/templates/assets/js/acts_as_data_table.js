// Generated by CoffeeScript 1.3.3
(function() {

  window.actsAsDataTable = {
    init: function() {
      return this.registerSortableLinks();
    },
    registerSortableLinks: function() {
      return jQuery(document).on("click", '.sortable-column-link', function(e) {
        var active, remote, url, urlAdd, urlRemove;
        urlAdd = jQuery(this).attr('data-url-add');
        urlRemove = jQuery(this).attr('data-url-remove');
        remote = jQuery(this).attr('data-remote') === 'true';
        active = jQuery(this).attr('data-active') === 'true';
        url = jQuery(this).attr('href');
        if (e.ctrlKey) {
          if (active) {
            url = urlRemove;
          } else {
            url = urlAdd;
          }
        }
        if (remote) {
          jQuery.ajax({
            'url': url,
            dataType: 'script'
          });
        } else {
          window.location.href = url;
        }
        return false;
      });
    }
  };

  jQuery(document).ready(function() {
    return actsAsDataTable.init();
  });

}).call(this);
